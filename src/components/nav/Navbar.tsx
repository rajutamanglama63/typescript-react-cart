import React from 'react'
import { StyledNavbar } from './Nav.styles'

type Props = {}

const Navbar = (props: Props) => {
  return (
    <StyledNavbar>Here is nav bar.</StyledNavbar>
  )
}

export default Navbar